# Feature flag — when "true", the workspace settings page exposes a "GitLab"
# tab that lets a workspace admin connect a GitLab project. Mirrors the
# server-side MULTICA_GITLAB_ENABLED flag.
VITE_GITLAB_ENABLED=false
